# Export PlantUML Diagram

To export the diagram images from the PlantUML source files, you can use the following command in your terminal or command prompt:

```shell
java -jar "path_to_.jar" -tpng -o "destination_folder" "source_file.puml"
```
